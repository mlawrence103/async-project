{
  "name": "poison-ivy-id",
  "version": "1.0.0",
  "description": "identify if uploaded image is poison ivy",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
